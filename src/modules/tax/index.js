const contracts = require('./contracts');
const policies = require('./policies');
const shared = require('./shared');

module.exports = {
  contracts,
  policies,
  shared,
  controllers: {},
  services: {},
  repositories: {},
  projections: {},
  routes: {},
};
