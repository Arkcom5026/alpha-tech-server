# Service Case Lifecycle

DRAFT -> OPEN -> TRIAGE -> DIAGNOSING -> WAITING_CUSTOMER / IN_SERVICE / WAITING_EXTERNAL -> QUALITY_CHECK -> READY_FOR_DELIVERY -> DELIVERED -> CLOSED. CANCELLED is terminal. Delivery and operational closure are separate.
